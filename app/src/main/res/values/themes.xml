<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 가 즉시 UI 를 그리므로 ActionBar 없는 기본 테마면 충분 -->
    <style name="Theme.CallStar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_bg</item>
    </style>

    <!-- 삭제 확인창 전용 투명 액티비티 (화면엔 시스템 삭제 다이얼로그만 보이게) -->
    <style name="Theme.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
